<h1>Editing Cate</h1>

<%= form_for @cate, :url => dashboard_cate_path, method: :patch do |f| %>
  <% if @cate.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@cate.errors.count, "error") %> prohibited this cate from being saved:</h2>

        <ul>
          <% @cate.errors.full_messages.each do |message| %>
              <li><%= message %></li>
          <% end %>
        </ul>
      </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<%= link_to 'Show', dashboard_cate_path(@cate) %> |
<%= link_to 'Back', dashboard_cates_path %>
